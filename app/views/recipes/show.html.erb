<div class="recipe-detail">
  <div class="recipe-header">
    <h2><%= @recipe.name %></h2>
    <% if @recipe.category %>
      <span class="category-badge large"><%= @recipe.category %></span>
    <% end %>
  </div>

  <div class="recipe-meta-bar">
    <% if @recipe.prep_time %>
      <div class="meta-item">
        <strong>⏱️ Prep Time:</strong>
        <%= @recipe.prep_time %> minutes
      </div>
    <% end %>
    <% if @recipe.cook_time %>
      <div class="meta-item">
        <strong>🔥 Cook Time:</strong>
        <%= @recipe.cook_time %> minutes
      </div>
    <% end %>
    <% if @recipe.servings %>
      <div class="meta-item">
        <strong>🍽️ Servings:</strong>
        <%= @recipe.servings %>
      </div>
    <% end %>
  </div>

  <div class="recipe-section">
    <h3>📝 Ingredients</h3>
    <div class="recipe-content">
      <%= simple_format(@recipe.ingredients) %>
    </div>
  </div>

  <div class="recipe-section">
    <h3>👨‍🍳 Instructions</h3>
    <div class="recipe-content">
      <%= simple_format(@recipe.instructions) %>
    </div>
  </div>

  <div class="recipe-actions">
    <%= link_to "← Back to Recipes", recipes_path, class: "btn" %>
    <%= link_to "Edit Recipe", edit_recipe_path(@recipe), class: "btn btn-secondary" %>
    <%= button_to "Delete Recipe", @recipe, method: :delete, form: { data: { turbo_confirm: "Are you sure you want to delete this recipe?" } }, class: "btn btn-danger" %>
</div>
